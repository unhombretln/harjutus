## Packages
(none needed)

## Notes
App uses existing shadcn/ui components already in client/src/components/ui.
No authentication; all fetch calls use credentials: "include" per template.
Set document title + meta description in AppShell (client-side).
Tailwind fontFamily already maps to CSS vars; no tailwind.config.ts changes required.
